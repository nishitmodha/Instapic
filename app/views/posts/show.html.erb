

<%= render 'posts/posts', post: @post %>
<% if @post.user.id == current_user.id %>
  <%= link_to 'Edit Post', edit_post_path(@post), class: "btn btn-dark" %>
  <%= link_to 'Cancel', posts_path, class: "btn btn-dark" %>
  <%= link_to 'Delete Post', post_path(@post), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-dark" %>
<% else %>
  <%= link_to 'Cancel', posts_path, class: "btn btn-dark" %>
<% end %>
